<%= render @post %>
<h3>Comments:--</h3>
<div id="comments">
  <%= render @post.comments %>
</div>
<%= link_to "New comment", new_post_comment_path(@post, format: :js),
            :remote => true, :id => 'new_comment_link' %>