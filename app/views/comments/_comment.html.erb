<%= div_for comment do%>
    <li><p><%= comment.text %></p></li>
    <p><%= time_ago_in_words comment.created_at%> ago</p>
    <p><%= link_to "Delete comment", [@post, comment], :method => :delete, data: { confirm: 'Are you sure?' }, remote: true, :class => "btn btn-danger"%></p>
<% end %>